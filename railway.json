{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm ci && npm run prisma:generate && npm run prisma:migrate:deploy && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm run start:prod"
  }
}